{
  "description": "Liquid declares {{ product.title }} but the enclosing h1 has no stable attr. Locus is resolvable, but no DOM selector can be built. Role falls into the inconclusive tier and ranks by base score alone — veto plays no part. This is the expected safe-fallback path when Liquid→DOM locus cannot be pinned.",
  "section_type": "product-information",
  "candidates": {
    "product_title": [
      { "id": "h1.product", "selector": "section.product-information h1", "baseScore": 0.74 },
      { "id": "h1.hero", "selector": "section.hero-banner h1", "baseScore": 0.61 }
    ]
  },
  "expected_winner": {
    "product_title": "h1.product"
  },
  "expected_tier": {
    "product_title": "inconclusive"
  }
}
